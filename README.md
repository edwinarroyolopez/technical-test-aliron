## Technical Test Aliron

## DB
    1. The database will be pointing to a server. (endpoint on .envs)

# SERVER
    1. add .env .env.dev .env.test here => server/ 
    2. cd server && npm i && npm run dev

# WEB
    1. cd web && npm i && npm run dev

